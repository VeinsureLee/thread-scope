/**
 * 【占位】SQLite 数据库连接与操作。
 * 依赖 better-sqlite3，届时添加。
 *
 * 计划表结构：
 * - thread: 帖子主表（id, board_name, title, author, content, ...）
 * - reply:  回复表（id, thread_id, floor, author, content, ...）
 */
// export function getDb(): Database.Database { ... }
